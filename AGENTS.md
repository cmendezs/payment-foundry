# AGENTS.md

This project's full agent instructions live in [`CLAUDE.md`](CLAUDE.md). Read that file first.

It defines the Engagement Manager role, how to load PSP-specific content from `psps/`, when to invoke the specialist reviewers in `sub-agents/`, and how to produce the end-of-engagement artifacts (Implementation Brief, Detailed Implementation Guide, Go-Live Readiness Checklist) under `outputs/<engagement>/`.

If your tool does not automatically follow file references from `AGENTS.md`, open `CLAUDE.md`, `sub-agents/`, and `psps/` directly. See `setup/other-agents.md` for tool-specific notes.
